# 更新日志

> English version: [CHANGELOG.en.md](CHANGELOG.en.md)

## v1.1.4（2026-09-11）

本次集中修复了一批会导致数据丢失或静默失效的问题，并提升了远程备份与界面刷新性能。

**数据安全（重要，建议升级）**

- **修复保留策略可能删掉最新备份**：备份文件名含设备名（`backupapp_<设备名>_<时间戳>`），原先按整个文件名排序会先比设备名——多台设备共用一个远程目录时，剪枝会把最新的一份删掉、却留着别的设备的旧备份。现在统一按快照时间戳排序
- **修复同一秒内重复备份互相覆盖**：快照名精确到秒，同一秒内连续备份两次会得到同一个文件名，后一份直接覆盖前一份（保留策略也只看到一份）。现在冲突时逐秒前移，两次备份都会保留
- **修复恢复失败后数据残缺**：恢复过程中出错时，原数据只剩在 `.old` 目录里，而源目录成了半成品。现在失败自动回滚，把原目录还原回原位；解压用的临时目录无论成败都会清理
- **修复目标目录在源目录内时恢复报错**：归档随源目录改名被一起搬走，解压时报"文件不存在"。改为先解压再改源目录，失败时源目录根本没被动过
- **修复备份自我包含**：目标目录位于源目录内时，历史备份会被再次打包进新备份，体积逐份膨胀。现在自动排除目标目录，并记一条日志
- **修复配置写入不原子**：`settings.json` 与应用定义原先直接截断重写，中途崩溃或断电会留下半截文件，导致应用无法启动。现改为临时文件 + `fsync` + 原子替换，且读取到损坏 JSON 时按"无数据"继续并记日志
- **修复失败的备份被当成有效备份**：压缩中断留下的半截归档会以正常文件名落盘，被保留策略计数、也可能被选中恢复。现先写 `.part`、成功后再原子改名
- **修复计划任务与手动备份可并发**：CLI（计划任务调用的入口）原先没加文件锁，可能与手动备份同时写同一批配置。现在所有备份/恢复入口统一加锁

**界面**

- **修复导入/导出点击无反应**：选项对话框的子控件在对话框关闭后被提前回收，取选项时抛异常（被 Qt 静默吞掉）。现已修复，导入/导出恢复正常
- **修复注册计划任务后状态一直显示"未注册"**：界面拿计划 ID 去比对，而系统任务名是 `BackupApp_<应用>_<计划>`，两者永不相等。现在按任务名正确比对
- **修复全局计划任务每次都提示"路径变更，需重新应用"**：`schtasks` 回读任务命令时会丢掉前导引号，原先的逐字比较因此永远不相等，误报为路径变更。现在容忍引号与空白差异，同时保留对真实路径变更的识别
- **修复暗黑模式下复选框看不清**：未勾选的复选框框线与深色背景几乎同色。现在按主题显式配色并新增勾选标记图标
- **修复编辑计划点"确定"后界面不刷新**：编辑后表格与任务状态不再更新，实际已保存
- **修复"恢复自身备份"按钮无反应**
- **修复刷新丢失浏览位置**：备份完成、任务注册后刷新会重建整张表格，导致选中行与滚动位置丢失。现改为增量更新
- **修复关闭窗口可能崩溃**：备份进行中关窗会销毁仍在运行的后台线程
- **修复计划任务注册/取消卡界面**：注册与取消注册改为后台执行（原先只有状态查询在后台）
- **修复自身备份文件列表**：切换协议时并发请求会让结果错乱，且进行中仍可重复触发恢复/删除

**性能**

- **远程备份与恢复更快**：复用 HTTP 连接（原先每次请求都重新握手）、目录探测结果缓存（原先进目录时逐级重复查询）
- **大文件恢复不再占满内存**：远程下载改为流式写入，原先把整个归档读进内存
- **自身备份多协议只打包一次**：启用多个协议时，同一份数据原先会按协议数重复打包压缩。现在按格式分组复用产物
- **导入/导出不再阻塞界面**
- **日志面板启动加载更快**：只从文件末尾按块读取，不再整份读入

**安全**

- **修复 tar.gz 解压路径穿越**：恶意/被篡改的归档可写到目标目录之外（自身备份会从远程下载归档后解压）。现在拒绝越界成员，7z 同样校验
- **修复 FTP 可能明文传凭据**：原先任何异常都当作"服务器不支持 TLS"降级明文。现在仅在 TLS 协商失败时降级，认证失败与网络错误照常报错

**其他修复**

- 备份前/后钩子超时时，终止整个进程树（原先只杀 shell，脚本拉起的子进程会残留）；钩子输出超长不再占用大量内存
- `.old` / `self_restore_old_*` 安全网目录只保留最近 3 份，不再无限增长
- 修复 link 模式可能误判真实目录为链接（有删除真实目录的风险）
- 修复自身备份文件数多报一个
- `crontab` 写入增加超时（Linux/macOS）
- 日志级别与最大日志大小设置现在真正生效（原先被忽略）
- 英文界面下"星期"下拉不再显示中文
- 自身备份/恢复的临时文件在失败时也会清理

## v1.1.3（2026-09-09）

- **修复启动/编辑计划卡顿**：系统任务注册状态改为后台线程查询（`schtasks /query` 冷查询可达数秒，原先会冻结界面），界面按钮与表格改为读取缓存，不再阻塞
- **修复暗黑主题弹窗白底**：明/暗主题新增与 QSS 一致的 QPalette，计划对话框等未命中 QSS 的容器不再退回浅色背景
- **备份前/后命令支持脚本文件**：可直接输入命令，也可点"浏览"选脚本文件——Windows 下 `.bat`/`.cmd`/`.ps1`（ps1 自动用 PowerShell 执行），macOS/Linux 下 `.sh`（用 sh 执行）
- **修复多个按钮点击无反应**：浏览/导入/导出/添加文件/保存脚本等对话框按钮在 i18n 改造后因 `_` 变量遮蔽翻译函数而静默失效，已全部修复
- **兼容修复**：`schtasks` 输出按系统 ANSI 码页解码（`mbcs`），避免 UTF-8 环境下任务状态查询崩溃

## v1.1.2（2026-09-06）

- **新增架构产物**：CI 现在构建 5 个平台产物 —— Windows x64、macOS x64 / ARM64、Linux x64 / ARM64（新增 macOS ARM64 与 Linux ARM64 原生构建）
- **修复 macOS x64 产物**：此前的 `backupapp-macos-x64` 误在 Apple Silicon runner 上构建（实为 ARM64 二进制），改用 Intel runner `macos-15-intel` 后恢复真正的 x64 构建
- macOS 签名公证流程改为同时覆盖两个 macOS 产物

## v1.1.1（2026-09-02）

- **切换语言自动重启**：语言切换后应用自动重启以新语言生效，无需手动重启
- **日志双语**：日志面板与 backup.log 的中文日志片段随界面语言切换（简体中文 / English）

## v1.1.0（2026-09-02）

- 新增：应用界面、CLI 与文档的双语支持（简体中文 / 英文）；新增 README、DEVELOPMENT、CHANGELOG 的英文版本

## v1.0.5（2026-08-24）

- **备份实时进度**：日志面板显示 `计划 [已处理/总数] 文件名`（150ms 节流防刷屏）
- **批量注册/取消计划任务**：改后台线程执行，不再逐条卡界面
- **备份进行中锁定导航**：应用列表/计划表格禁用，防止误操作触发刷新
- **备份大小可读化**：`B/KB/MB/GB` 格式替换原始字节数（GUI 日志/CLI/日志文件统一）

## v1.0.4（2026-08-23）

- **GUI 性能优化**：计划任务状态查询与已注册列表合并为一次缓存查询（5 秒 TTL），`schtasks` 子进程增加 20 秒超时；切换应用、备份完成后界面不再卡顿
- **日志面板**：新日志追加时自动滚动到底部（含启动加载尾部日志）
- **scoop 升级兼容**：注册计划任务时自动把版本目录（`apps\backupapp\1.0.4\...`）换成 `current` 链接，`scoop update` 后计划任务不再失效（已有任务需在界面重新点一次"注册"）
- **修复**：schtasks 中文表头 / 部分系统多主机名列时的任务解析错误

## v1.0.3

- 备份计划命令钩子（备份前/后 + 超时）
- 数值框滚轮锁定（防误改）

## v1.0.2

- 导入/导出加密与覆盖选项
- FTP/SFTP 网关兼容（TLS 降级、PASV 重试、流式传输）
- 恢复覆盖策略（覆盖/仅新增）

## v1.0.1

- 自身备份/恢复功能
- 多协议独立配置（WebDAV/S3/FTP/SFTP）
- 远程网关兼容（OpenList WebDAV、CDN/OSS S3）

## v1.0.0

- 首个正式版：备份/恢复、压缩加密、保留策略、计划任务、脚本生成