# 更新日志

> English version: [CHANGELOG.en.md](CHANGELOG.en.md)

## v1.1.3（2026-09-09）

- **修复启动/编辑计划卡顿**：系统任务注册状态改为后台线程查询（`schtasks /query` 冷查询可达数秒，原先会冻结界面），界面按钮与表格改为读取缓存，不再阻塞
- **修复暗黑主题弹窗白底**：明/暗主题新增与 QSS 一致的 QPalette，计划对话框等未命中 QSS 的容器不再退回浅色背景
- **备份前/后命令支持脚本文件**：可直接输入命令，也可点"浏览"选脚本文件——Windows 下 `.bat`/`.cmd`/`.ps1`（ps1 自动用 PowerShell 执行），macOS/Linux 下 `.sh`（用 sh 执行）
- **修复多个按钮点击无反应**：浏览/导入/导出/添加文件/保存脚本等对话框按钮在 i18n 改造后因 `_` 变量遮蔽翻译函数而静默失效，已全部修复
- **兼容修复**：`schtasks` 输出按系统 ANSI 码页解码（`mbcs`），避免 UTF-8 环境下任务状态查询崩溃

## v1.1.2（2026-09-06）

- **新增架构产物**：CI 现在构建 5 个平台产物 —— Windows x64、macOS x64 / ARM64、Linux x64 / ARM64（新增 macOS ARM64 与 Linux ARM64 原生构建）
- **修复 macOS x64 产物**：此前的 `backupapp-macos-x64` 误在 Apple Silicon runner 上构建（实为 ARM64 二进制），改用 Intel runner `macos-15-intel` 后恢复真正的 x64 构建
- macOS 签名公证流程改为同时覆盖两个 macOS 产物

## v1.1.1（2026-09-02）

- **切换语言自动重启**：语言切换后应用自动重启以新语言生效，无需手动重启
- **日志双语**：日志面板与 backup.log 的中文日志片段随界面语言切换（简体中文 / English）

## v1.1.0（2026-09-02）

- 新增：应用界面、CLI 与文档的双语支持（简体中文 / 英文）；新增 README、DEVELOPMENT、CHANGELOG 的英文版本

## v1.0.5（2026-08-24）

- **备份实时进度**：日志面板显示 `计划 [已处理/总数] 文件名`（150ms 节流防刷屏）
- **批量注册/取消计划任务**：改后台线程执行，不再逐条卡界面
- **备份进行中锁定导航**：应用列表/计划表格禁用，防止误操作触发刷新
- **备份大小可读化**：`B/KB/MB/GB` 格式替换原始字节数（GUI 日志/CLI/日志文件统一）

## v1.0.4（2026-08-23）

- **GUI 性能优化**：计划任务状态查询与已注册列表合并为一次缓存查询（5 秒 TTL），`schtasks` 子进程增加 20 秒超时；切换应用、备份完成后界面不再卡顿
- **日志面板**：新日志追加时自动滚动到底部（含启动加载尾部日志）
- **scoop 升级兼容**：注册计划任务时自动把版本目录（`apps\backupapp\1.0.4\...`）换成 `current` 链接，`scoop update` 后计划任务不再失效（已有任务需在界面重新点一次"注册"）
- **修复**：schtasks 中文表头 / 部分系统多主机名列时的任务解析错误

## v1.0.3

- 备份计划命令钩子（备份前/后 + 超时）
- 数值框滚轮锁定（防误改）

## v1.0.2

- 导入/导出加密与覆盖选项
- FTP/SFTP 网关兼容（TLS 降级、PASV 重试、流式传输）
- 恢复覆盖策略（覆盖/仅新增）

## v1.0.1

- 自身备份/恢复功能
- 多协议独立配置（WebDAV/S3/FTP/SFTP）
- 远程网关兼容（OpenList WebDAV、CDN/OSS S3）

## v1.0.0

- 首个正式版：备份/恢复、压缩加密、保留策略、计划任务、脚本生成